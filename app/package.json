{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start":"ts-node ./index.ts",
    "dev": "ts-node-dev --no-notify --respawn --transpile-only --trace-warnings -- ./src",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^14.14.33",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.3"
  }
}
