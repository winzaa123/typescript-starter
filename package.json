{
    "name" : "typescript-starter",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "dev": "yarn --cwd ./app dev",
        "build": "yarn --cwd ./app build",
        "start": "yarn --cwd ./app start"
    }
}